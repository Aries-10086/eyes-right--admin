# Eyes Right — 区域采集实时贴眼落地计划书

## 1. 目标

在用户框选的屏幕区域内，持续采集画面帧，本地检测猫/狗眼睛（或后续扩展人脸），并将眼罩/贴图以**透明置顶层**叠在该区域对应位置上，实现「看视频时同步贴眼」的效果。

**不做：** 云端上传、改播放器内核、整屏无差别采集（默认）、录制导出成片（可作二期）。

---

## 2. 范围与非目标

| 纳入（一期） | 不纳入（一期） |
|-------------|----------------|
| macOS 13+ Apple Silicon | Windows 区域采集（二期对齐） |
| 用户拖拽框选采集区域 | 自动识别播放器窗口 |
| 区域帧采集 + 现有 ONNX 眼点检测 | 单独检一只眼的新模型 |
| 透明置顶 overlay 跟眼点 | 音频驱动、字幕联动 |
| 「啊啊啊」「加一道光」两种贴图模式复用 | 正式公证签名（可沿用 adhoc） |

---

## 3. 总体架构

```
用户框选区域 (Screen Region)
        ↓
区域帧采集 (CGDisplayStream / ScreenCaptureKit)
        ↓
帧缩放 / letterbox → ONNX 姿态检测 (复用现有 pet_eye_best.onnx)
        ↓
眼点坐标（区域相对坐标）→ 映射到屏幕全局坐标
        ↓
透明置顶 Overlay 窗口按坐标绘制贴图
```

与现有静态图链路对比：

| 环节 | 现有（静态图） | 本期（区域采集） |
|------|----------------|------------------|
| 输入 | 文件 / 拖图 | 屏幕矩形区域逐帧 |
| 检测 | 单次 | 连续（降频，如 10–15 FPS） |
| 输出 | 另存图片 | 置顶透明窗实时画 |
| 坐标 | 图像像素 | 区域局部 → 屏幕全局 |

---

## 4. 技术选型（Mac）

| 模块 | 推荐方案 | 说明 |
|------|----------|------|
| 采集 | **ScreenCaptureKit**（优先）或 CGDisplayStream | macOS 12.3+；需「屏幕录制」权限 |
| 选区 UI | 全屏半透明蒙版 + 拖拽矩形 | 类似截图选区体验 |
| 推理 | 复用 `PoseDetector` + `pet_eye_best.onnx` | 降分辨率 letterbox 到 640 |
| Overlay | `NSPanel` / `NSWindow`：`floating` + `ignoresMouseEvents` + 透明 | 不挡点击（可选开关：点击穿透） |
| 贴图 | 复用 `EyeOverlay`（啊啊啊 / 加一道光） | 坐标从区域帧映射到屏 |

权限：

- 系统设置 → 隐私与安全性 → **屏幕录制** → 勾选 Eyes Right  
- 首次启动引导页说明原因（本地处理、不上传）

---

## 5. 功能拆解

### 5.1 选区

1. 菜单/按钮：「区域贴眼」  
2. 进入选区模式：屏幕变暗，拖出矩形  
3. 确认后显示细边框（可拖动四角微调、可移动整体）  
4. 支持「锁定区域」后开始采集  

### 5.2 采集与检测

1. 按区域裁剪帧（或采集时指定 crop）  
2. 推理降频：**默认 12 FPS**（可调 8 / 12 / 15），避免拖垮播放与发热  
3. 检测失败时：保持上一帧有效眼点 N 帧（如 5～8 帧）再淡出，减少闪烁  
4. 置信度阈值沿用 `conf=0.15`；过低则不画  

### 5.3 Overlay

1. 全屏或区域同尺寸的透明置顶窗  
2. 眼点从「区域图像坐标」→「屏幕坐标」：  
   `screen = region.origin + localPoint`  
3. 模式切换：啊啊啊 / 加一道光（与静态版一致）  
4. 快捷键：开始/暂停、显示/隐藏 overlay、退出  

### 5.4 性能与体验

| 项 | 指标（一期目标） |
|----|------------------|
| 端到端延迟 | ≤ 120–200 ms（观感可接受） |
| CPU/GPU | 插电可连续 30 分钟不明显卡顿视频 |
| 发热 | 笔记本可接受；提供「省电档」降到 8 FPS |
| 失败提示 | 无权限 / 未检出 / 区域过小 有明确文案 |

---

## 6. 里程碑与排期（建议）

按 1 人、熟悉现有 Mac 工程估算：

| 阶段 | 内容 | 预估 |
|------|------|------|
| M0 设计确认 | 权限文案、UI 草图、FPS/降级策略定稿 | 0.5 天 |
| M1 选区 + 采集 | 框选、权限、拿到区域 CVPixelBuffer/CGImage | 2 天 |
| M2 检测接入 | 帧 → letterbox → PoseDetector → 眼点；降频与平滑 | 1.5 天 |
| M3 Overlay | 透明置顶、坐标映射、两种贴图模式 | 2 天 |
| M4 打磨 | 快捷键、边框、失败态、省电档、内存泄漏检查 | 1.5 天 |
| M5 验收打包 | 真机测播放器场景、打 0.3.0 DMG、更新说明 | 1 天 |

**合计约 8–9 个工作日**（不含 Windows）。

版本建议：功能合入后发 **v0.3.0**（区域采集为新能力）。

---

## 7. 验收标准

1. 用户可框选区域并开始/停止采集  
2. 未授权屏幕录制时有引导，授权后可工作  
3. 区域内有清晰正脸宠物时，overlay 大致贴在双眼（允许轻微抖动）  
4. 切换「啊啊啊 / 加一道光」实时生效  
5. Overlay 默认点击穿透，不妨碍暂停/拖进度条（可关）  
6. 退出后采集与 overlay 全部释放，无残影窗  
7. 完全本地，无网络请求  

**推荐验收场景：** 本机视频文件用 QuickTime / IINA 播放猫狗正脸片段；浏览器本地 HTML5 video（注意部分流媒体 DRM 可能黑帧，作风险项）。

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| DRM / 受保护内容采集为黑帧 | 网飞等无法贴 | 文档写明；验收用本地文件 |
| 侧脸、遮挡、小脸 | 检不出或抖 | 保持上一帧；提示换角度；后期再训模型 |
| 全屏播放与菜单栏/刘海 | 选区坐标偏移 | 用 ScreenCaptureKit 的显示坐标系统一换算 |
| 发热与掉帧 | 体验差 | 默认 12 FPS + 省电档；区域过大时提示缩小 |
| adhoc 签名 + 屏幕录制权限 | 分发麻烦 | 说明里写清授权步骤；长期上 Developer ID |

---

## 9. 目录与代码落点（建议）

在现有 `EyesRightMac/` 上扩展，不另起仓库：

```
EyesRightMac/Sources/EyesRightMac/
  Capture/
    RegionPicker.swift      # 选区
    RegionCaptureSession.swift
  Overlay/
    FloatingOverlayWindow.swift
    OverlayRenderer.swift   # 调 EyeOverlay
  Live/
    LivePipeline.swift      # 帧调度 + 降频 + 平滑
  ...
```

静态图流程保持不动；「区域贴眼」为独立入口。

---

## 10. 文档与权限说明（需同步写）

在 `如何打开.txt` 或新文档中增加：

1. 系统设置 → 隐私与安全性 → 屏幕录制 → 允许 Eyes Right  
2. 建议用本地视频验收  
3. 区域尽量只框人脸，减少算力  

---

## 11. 二期（可选）

- Windows：DXGI Desktop Duplication / Windows.Graphics.Capture 做区域采集  
- 自动吸附播放器窗口  
- 录制带 overlay 的成片导出  
- 人脸模型 / 单眼模型  
- Developer ID + 公证，降低权限与 Gatekeeper 摩擦  

---

## 12. 决策清单（开工前确认）

- [ ] 一期仅 Mac，Windows 是否跟版  
- [ ] Overlay 默认是否点击穿透  
- [ ] 默认 FPS（建议 12）  
- [ ] 目标版本号（建议 0.3.0）  
- [ ] 「加一道光」素材是否沿用当前 `guang_overlay.jpg`  

---

**一句话：** 用区域采集拿帧 → 复用现有 ONNX 检眼 → 透明置顶按坐标贴图；先做 Mac，约 8–9 天可验收一版可演示能力。
