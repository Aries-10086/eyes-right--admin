# 照片识眼 + 叠加眼睛素材 — 落地步骤（小模型直训）

## 1. 目标

输入**猫或狗**照片 → **小模型**输出每张脸的左右眼中心 → 把透明 PNG 贴上去 → 输出合成图。

**范围：猫狗都做，同一个模型，不拆两个网络。**

```
原图 → YOLOv8n-pose（宠物脸框 + 两眼 + 鼻）→ 用两眼做相似变换 → 透明叠加 → 输出
```

---

## 2. 已有资源

| 文件 | 用途 |
|------|------|
| `IMG_20260819_142559_cutout.png` | 透明眼睛素材（猫狗共用） |
| `dataset/pet_face_detection_dataset_1/` | 已下载：约 1.5 万张猫狗脸，框 + 两眼 + 鼻 |

素材常量（写进代码）：

```python
OVERLAY_LEFT_EYE  = (200, 220)
OVERLAY_RIGHT_EYE = (671, 228)
OVERLAY_EYE_DIST  = 471
```

---

## 3. 模型选型（只用这一个）

**YOLOv8n-pose** — 最小 pose 模型，一张图同时输出：

- 脸框 `bbox`（猫、狗都标成同一类 `pet`）
- 关键点 0：左眼中心
- 关键点 1：右眼中心
- 关键点 2：鼻子（训练用，贴图不用）

| 项 | 值 |
|----|-----|
| 预训练权重 | `yolov8n-pose.pt` |
| 输入尺寸 | `640×640` |
| 参数量 | ~3M |
| CPU 推理 | ~100–300ms/张 |
| 导出 | ONNX → 本地 / 移动端 |

> 不做两阶段（先检脸再回归），不依赖 MediaPipe，**一个模型训到底**。

---

## 4. 落地步骤

### Step 1：环境

```bash
cd /Users/mac/Desktop/画眼睛
pip3 install ultralytics opencv-python pillow numpy onnx onnxruntime
```

验证：

```bash
python3 -c "from ultralytics import YOLO; print('ok')"
yolo pose predict model=yolov8n-pose.pt source=https://ultralytics.com/images/bus.jpg
```

---

### Step 2：准备数据

#### 2.1 标注格式（YOLO Pose）

每张图一个 `.txt`，与图片同名：

```
# class  cx  cy  w  h  kpt0_x  kpt0_y  kpt0_vis  kpt1_x  kpt1_y  kpt1_vis
0  0.512  0.481  0.352  0.412  0.423  0.392  2  0.601  0.395  2
```

- 坐标全部 **0~1 归一化**（相对图宽高）
- `kpt_vis`：`0`=未标注，`2`=可见
- 一张图多张脸 → 多行
- 关键点顺序固定：`0=左眼，1=右眼`（若保留鼻子则为 `2=鼻`）
- **不要过滤猫或狗**：Zenodo 这份本身就是猫狗混合，原样用

#### 2.2 数据从哪来

| 方式 | 说明 |
|------|------|
| **主数据（已有）** | `dataset/pet_face_detection_dataset_1/`：train 13500 / val 750 / test 750 |
| 补数据 | 自己拍短吻猫、扁脸狗、侧脸、室内暗光，各补几十张即可 |
| 不要用 | WFLW / 300W（那是**人脸**，会把模型带偏） |

#### 2.3 数据量

| 阶段 | 量 |
|------|-----|
| 能跑通 | 直接用现成 train+val，**够训第一版** |
| 线上可用 | 现成 1.5 万 + 少量自家猫狗照片 |
| 划分 | 沿用数据集自带 train / val / test |

**类别：`nc: 1`，名字 `pet`。** 贴眼睛不需要区分猫还是狗；拆成两类不会让眼点更准，只会让数据更碎。

#### 2.4 目录

```
dataset/
├── images/
│   ├── train/
│   └── val/
├── labels/
│   ├── train/
│   └── val/
└── eye_pose.yaml
```

`eye_pose.yaml`：

```yaml
path: ./dataset
train: images/train
val: images/val

nc: 1
names: ['pet']      # 猫狗同一类

kpt_shape: [3, 3]   # 左眼、右眼、鼻；每个 x y vis
flip_idx: [1, 0, 2] # 翻转时左右眼互换，鼻子不动
```

---

### Step 3：训练

```bash
# 从头训（推荐）
yolo pose train \
  data=dataset/eye_pose.yaml \
  model=yolov8n-pose.pt \
  epochs=150 \
  imgsz=640 \
  batch=16 \
  patience=30 \
  project=runs/pose \
  name=eye_v1

# 看 val 曲线：box_loss / pose_loss 都收敛再停
```

调参备忘：

| 现象 | 处理 |
|------|------|
| 眼点飘 | 加数据、降 `lr0`、多训 50 epoch |
| 小脸检不出 | 加小脸样本、或用 `yolov8s-pose` |
| 过拟合 | 加增强 `hsv_h/s/v`、`degrees=10`、`fliplr=0.5` |
| 左右眼反 | 检查标注顺序和 `flip_idx` |

---

### Step 4：评估

```bash
yolo pose val model=runs/pose/eye_v1/weights/best.pt data=dataset/eye_pose.yaml
```

| 指标 | 通过线 |
|------|--------|
| pose mAP50 | > 0.85（验证集） |
| 眼中心误差 | 预测 vs 标注 < **5% 眼间距** |
| 叠加主观 | 抽 50 张合成，> 4/5 满意 |

误差脚本逻辑：

```python
error = dist(pred_left, gt_left) + dist(pred_right, gt_right)
error /= (2 * eye_distance)   # 归一化到眼间距比例
```

---

### Step 5：导出

```bash
yolo export model=runs/pose/eye_v1/weights/best.pt format=onnx opset=12 simplify=True
# 输出：runs/pose/eye_v1/weights/best.onnx
```

---

### Step 6：推理 → 叠加（核心代码逻辑）

```python
from ultralytics import YOLO
import cv2
import numpy as np

model = YOLO("weights/eye_best.onnx")   # 或 .pt
overlay = cv2.imread("IMG_20260819_142559_cutout.png", cv2.IMREAD_UNCHANGED)

OL, OR = (200, 220), (671, 228)         # 素材眼中心

def detect_eyes(image_bgr):
    results = model(image_bgr, verbose=False)[0]
    pairs = []
    if results.keypoints is None:
        return pairs
    for kpts, box in zip(results.keypoints.xy, results.boxes.xyxy):
        if len(kpts) < 2:
            continue
        left  = tuple(kpts[0].tolist())   # kpt0 = 左眼
        right = tuple(kpts[1].tolist())   # kpt1 = 右眼
        if left[0] > right[0]:
            left, right = right, left
        pairs.append((left, right))
    return pairs

def similarity_matrix(src_l, src_r, dst_l, dst_r):
    # src = 素材眼点，dst = 照片眼点
    # → 2×3 仿射矩阵（缩放+旋转+平移）
    ...

def overlay_eyes(image_bgr, pairs):
    for left, right in pairs:
        M = similarity_matrix(OL, OR, left, right, scale_boost=1.2)
        warped = cv2.warpAffine(overlay, M, (image_bgr.shape[1], image_bgr.shape[0]))
        image_bgr = alpha_blend(image_bgr, warped)
    return image_bgr
```

**Step 4 相似变换公式**（不变）：

```
Oc = (OL+OR)/2          Pc = (PL+PR)/2
scale = |PR-PL|/|OR-OL| × 1.15~1.25
angle = atan2(PR-PL) - atan2(OR-OL)
P' = Pc + scale · R(angle) · (P - Oc)
```

---

### Step 7：命令行

```bash
python3 overlay_eyes.py 输入.jpg \
  --weights weights/eye_best.onnx \
  -o 输出.jpg \
  --scale-boost 1.2 \
  --preview debug.jpg
```

---

### Step 8：测试清单

| 场景 | 预期 |
|------|------|
| 猫正脸 / 狗正脸 | 眼睛对齐，大小合适 |
| 短吻（英短、巴哥）与长吻（暹罗、柯基） | 两眼间距对，素材不飘 |
| 侧脸 ≤30° | 素材随脸旋转 |
| 猫狗合照 | 各贴一对 |
| 闭眼 / 遮挡 | 置信度低则跳过 |
| 无 GPU 手机 | ONNX CPU < 500ms |

---

## 5. 目录结构

```
画眼睛/
├── dataset/
│   ├── images/{train,val}/
│   ├── labels/{train,val}/
│   └── eye_pose.yaml
├── tools/
│   ├── convert_pet_labels.py # Zenodo 标注接到 yaml
│   └── eval_eye_error.py     # 眼中心误差
├── runs/pose/eye_v1/weights/
│   ├── best.pt
│   └── best.onnx
├── weights/eye_best.onnx     # 部署用副本
├── overlay_eyes.py           # 推理 + 叠加
├── IMG_20260819_142559_cutout.png
└── 落地步骤.md
```

---

## 6. 全流程对照

| 步骤 | 内容 | 工时 |
|------|------|------|
| Step 1 | 装 ultralytics | 0.5h |
| Step 2 | 把已有猫狗标注接到 yaml（几乎不用手标） | 0.5 天 |
| Step 3 | 训练 yolov8n-pose | 0.5~1 天（GPU） |
| Step 4 | val + 眼点误差 | 0.5 天 |
| Step 5 | 导出 ONNX | 0.5h |
| Step 6 | 接入 overlay | 0.5 天 |
| Step 7~8 | 测试调参 + 补 bad case | 2~3 天 |

**MVP 总计：约 3~5 天**（数据已齐；有 GPU 更快）。评估时 **猫、狗各抽 25 张** 看叠加效果。

---

## 7. MVP 验收

- [ ] `best.onnx` 在验证集 pose mAP50 > 0.85
- [ ] 眼中心误差 < 5% 眼间距
- [ ] 猫、狗正脸合成都肉眼对齐
- [ ] 单张 1080p CPU 推理 + 叠加 < 1s
- [ ] 检测失败有中文提示

---

## 8. 常见问题

**Q：只有 CPU 能训吗？**  
A：能，但慢；建议 Colab / 云 GPU 训，本地 ONNX 推理。

**Q：要不要先检脸再回归？**  
A：不用。YOLOv8n-pose 一次出 bbox + 眼点，更简单。

**Q：素材贴偏了？**  
A：调 `--scale-boost`；确认标注 kpt0=左眼、kpt1=右眼。

**Q：换素材？**  
A：重新量 PNG 两眼中心，改 `OL / OR` 常量即可，**模型不用重训**。

**Q：模型不够准？**  
A：补 500 张 bad case 重新训，或换 `yolov8s-pose`（稍大稍准）。

**Q：猫狗要不要两个模型？**  
A：不要。一份数据、一个 `pet` 类、一个 YOLOv8n-pose 即可。

**Q：猫狗眼睛形状差很多，一张素材行吗？**  
A：行。模型只负责眼点位置；素材缩放跟着两眼间距走。若某品种贴太大/太小，只调 `--scale-boost`，不必分模型。
